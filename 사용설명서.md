# 회의에서 살아남기 - 사용 설명서

---

## 목차
1. 폴더 구조
2. 로컬에서 실행하기 (내 컴퓨터에서 테스트)
3. 관리자 페이지 사용법
4. Render에 배포하기

---

## 1. 폴더 구조

프로젝트를 열면 이런 파일들이 있어요:

```
survive_meeting/          ← 프로젝트 폴더
├── converter/            ← 실제 서비스 코드
│   ├── admin.py          ← 관리자 페이지 설정
│   ├── models.py         ← 데이터베이스 구조
│   └── views.py          ← 변환 로직
├── survive_meeting/      ← 프로젝트 설정
│   ├── settings.py       ← 환경 설정
│   └── urls.py           ← URL 경로
├── templates/
│   └── converter/
│       └── index.html    ← 화면 디자인
├── manage.py             ← Django 실행 파일
├── requirements.txt      ← 필요한 패키지 목록
├── build.sh              ← Render 배포용 스크립트
└── db.sqlite3            ← 데이터베이스 (자동 생성됨)
```

---

## 2. 로컬에서 실행하기

### 2-1. 터미널(명령 프롬프트) 열기
- **Windows**: 시작 버튼 → "cmd" 검색 → 명령 프롬프트 실행
- **Mac**: Spotlight(⌘+Space) → "터미널" 검색

### 2-2. 프로젝트 폴더로 이동
```
cd 프로젝트폴더경로
예: cd C:\Users\홍길동\survive_meeting
```

### 2-3. 필요한 패키지 설치
```
pip install -r requirements.txt
```

### 2-4. 데이터베이스 초기화
```
python manage.py migrate
```

### 2-5. 관리자 계정 만들기
```
python manage.py createsuperuser
```
→ 이름(username), 이메일, 비밀번호를 입력하라고 나와요.
→ 이게 admin 페이지 로그인 정보가 됩니다.

### 2-6. 서버 실행
```
python manage.py runserver
```

### 2-7. 브라우저에서 확인
- 서비스 화면: http://127.0.0.1:8000
- 관리자 화면: http://127.0.0.1:8000/admin

> **서버 끄는 법**: 터미널에서 Ctrl+C 누르기

---

## 3. 관리자 페이지 사용법

### 접속
http://127.0.0.1:8000/admin 에서 createsuperuser로 만든 ID/PW로 로그인

### 템플릿 추가/수정/삭제
1. "변환 템플릿 목록" 클릭
2. "변환 템플릿 추가" 버튼 클릭
3. 아래 항목 입력:

| 항목 | 설명 | 예시 |
|------|------|------|
| 템플릿 이름 | 구분용 이름 | "하기 싫다 버전" |
| 키워드 | 쉼표로 구분 | 싫어,하기 싫,하고 싶지 않 |
| 말투 | 드롭다운에서 선택 | 부드럽게 |
| 변환 결과 1 | 필수 | 현재 업무 우선순위 상 조율이 필요합니다. |
| 변환 결과 2 | 선택 | 일정 재검토가 필요한 사항입니다. |
| 변환 결과 3 | 선택 | 역할 분담 재논의를 제안드립니다. |
| 활성화 | 체크 = 사용중 | ✅ 체크 |

> **키워드 동작 방식**: 사용자가 입력한 텍스트에 키워드가 하나라도 포함되면 이 템플릿의 결과가 나옵니다.

---

## 4. Render에 배포하기

### 4-1. GitHub에 코드 올리기
1. github.com 회원가입/로그인
2. New Repository 만들기 (이름 예: survive-meeting)
3. 로컬 폴더에서 아래 명령 실행:
```
git init
git add .
git commit -m "첫 배포"
git remote add origin https://github.com/내아이디/survive-meeting.git
git push -u origin main
```

### 4-2. Render에서 배포
1. render.com 회원가입/로그인
2. "New" → "Web Service" 클릭
3. GitHub 연결 후 방금 만든 저장소 선택
4. 아래처럼 설정:

| 항목 | 값 |
|------|---|
| Runtime | Python |
| Build Command | `./build.sh` |
| Start Command | `gunicorn survive_meeting.wsgi` |

5. "Environment Variables" 탭에서 아래 추가:

| KEY | VALUE |
|-----|-------|
| SECRET_KEY | 아무 랜덤 문자열 (예: abc123xyz789qwerty) |
| DEBUG | False |

6. "Create Web Service" 클릭
7. 배포 완료되면 Render가 URL을 알려줍니다! (예: https://survive-meeting.onrender.com)

### 4-3. 배포 후 관리자 계정 만들기
Render 대시보드 → Shell 탭 → 아래 명령 실행:
```
python manage.py createsuperuser
```

---

## 자주 있는 문제

**Q: `pip`을 찾을 수 없다고 해요**
→ `pip3 install -r requirements.txt` 또는 `python -m pip install -r requirements.txt` 시도

**Q: 포트가 이미 사용 중이라고 해요**
→ `python manage.py runserver 8080` (포트 번호 바꾸기)

**Q: Render 배포 후 관리자 페이지가 이상해 보여요**
→ `build.sh`의 `collectstatic`이 실행됐는지 배포 로그 확인

---

> 만든 사람이 코딩을 처음 배우는 중이라면, 에러가 나도 당황하지 마세요.
> 에러 메시지를 복사해서 Claude에게 물어보면 해결됩니다 😊
